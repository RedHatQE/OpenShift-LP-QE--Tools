# AI History Log
This file tracks important changes and decisions in this directory and its sub-directories (when sensible).

## 2026-05-10
### EnsureReqs downloads use curl only
- `common/EnsureReqs.sh` installs jq, yq, and chisel with `curl -fsSL` (file writes use `-o`; chisel installer streams to stdout then pipes to bash).
- Avoids requiring `wget` in CI/container images that only ship `curl`.

## 2026-03-10
### Initial Common Library Setup
- Created `common/EnsureReqs.sh` (moved from `ci-operator/common/PreAction--EnsureReqs.sh`, renamed, generalized from CI Operator context).
- Created `common/BuildCustomScriptsFromYAML.sh` (builds compounded sub-shell scripts from YAML input, supports array and map modes).
